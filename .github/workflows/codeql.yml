name: CodeQL

on:
  workflow_dispatch:

jobs:
  unsupported-language:
    runs-on: ubuntu-latest
    steps:
      - name: Explain skip
        run: |
          echo "This repository is a Flutter/Dart project."
          echo "GitHub CodeQL does not currently provide first-class Dart analysis."
          echo "Keep this workflow file for baseline consistency and re-enable CodeQL if supported languages are added later."
